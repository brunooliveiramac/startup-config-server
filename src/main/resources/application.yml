server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/brunooliveiramac/startup-job-config-server
          search-paths:
            - config
  security:
    user:
      name: startup
      password: startup
    basic:
      enabled: true
